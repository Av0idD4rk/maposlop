# План развития CTF-карты

## 1. Цель продукта

Сделать понятную и живую карту CTF, которая помогает школьникам и начинающим командам:

- находить подходящие соревнования рядом и онлайн;
- быстро понимать формат, ограничения, даты и правила участия;
- видеть активность CTF-сообщества не только в столицах, но и в малых городах;
- узнавать о командах своего города и их результатах;
- предлагать новые события, которые проходят модерацию перед публикацией.

Главная продуктовая метрика — число переходов к регистрации на CTF из малых и средних городов. Дополнительные метрики: доля регионов с актуальными событиями, число добавленных организаторами событий, конверсия карточка → регистрация, использование фильтров и доля импортов, прошедших модерацию без исправлений.

## 2. Что есть сейчас

MVP уже умеет:

- показывать интерактивную 3D-карту регионов России;
- загружать опубликованные будущие CTF из Django API;
- группировать флаги по региону и раскрывать их при выборе региона;
- показывать описание, организатора, место и ссылку на регистрацию;
- принимать предложения пользователей через публичную форму;
- модерировать предложения в стандартной Django Admin;
- учитывать `prefers-reduced-motion` и базовые мобильные размеры.

Текущий стек: Django 5.1.7, SQLite, Three.js, TypeScript и Vite. Код физически разделён на `backend/` и `frontend/`; frontend-сборка попадает в `backend/static/dist/`. В проекте 3 backend-теста; отдельного линтера, CI и автоматических браузерных тестов нет.

### Обновление после pull от 12.08.2026

- Получены коммиты `87faaa4` и `e0ac1bf`: Django перенесён в `backend/`, Vite/TypeScript — в `frontend/`.
- Бизнес-логика, модели, API и пользовательские возможности не изменились, поэтому функциональные этапы ниже остаются актуальными.
- Разделение каталогов считается завершённой архитектурной подготовкой: новые backend/frontend-модули нужно создавать только в соответствующей части репозитория.
- Команды запуска и CI теперь должны явно задавать рабочие каталоги: Django-команды выполняются в `backend/`, npm-команды — в `frontend/`.
- На чистом checkout `manage.py check` выдаёт `staticfiles.W004`, пока frontend ещё не создал `backend/static/`. Нужно устранить зависимость от порядка команд — например, хранить родительский каталог или формировать `STATICFILES_DIRS` только из существующих путей — и отдельно проверить сценарий чистой установки.

## 3. Результаты аудита и первоочередные исправления

### P0 — до любого публичного развёртывания

1. Обновить Django с неподдерживаемой ветки `5.1.7` на актуальный патч поддерживаемой LTS-ветки `5.2` и повторить тесты. `pip-audit` на 12.08.2026 нашёл 24 записи об известных уязвимостях в текущей версии. Не фиксировать в плане конкретный патч навсегда: при реализации брать последний доступный security patch ветки 5.2.
2. Разделить настройки на base/dev/prod или читать их из окружения: `SECRET_KEY`, `DEBUG`, `ALLOWED_HOSTS`, URL БД, `CSRF_TRUSTED_ORIGINS`. Сейчас секрет для разработки и `DEBUG=True` зашиты в `backend/ctfmap/settings.py`.
3. Для production включить HTTPS-настройки после настройки TLS/proxy: secure cookies, SSL redirect и HSTS с безопасным поэтапным увеличением срока. `manage.py check --deploy` сейчас выдаёт 6 предупреждений.
4. Перейти на PostgreSQL перед импортами, историей результатов и лидербордами; SQLite оставить допустимым только для локальной разработки.
5. Защитить `/api/submissions/` от спама и исчерпания диска: rate limit по IP/сессии, honeypot или CAPTCHA после порога, ограничение размера тела, журналирование и политика хранения контактных данных.
6. Добавить CI: установка из `backend/requirements.txt` и `frontend/package-lock.json`, frontend build, затем Django check/проверка миграций/backend-тесты, audits зависимостей и браузерный smoke-тест. Отдельный job должен проверять чистый checkout, чтобы не зависеть от оставшихся локальных артефактов.

Критерий готовности P0: production-конфигурация проходит `check --deploy` без необоснованных предупреждений, dependency audit не содержит известных исправимых уязвимостей высокого риска, а CI стабильно зелёный.

### P1 — подтверждённые дефекты и долги MVP

1. Развести административный регион и визуальную группу карты. Сейчас коды Москвы (`77`) заменяются Московской областью (`50`), Санкт-Петербурга (`78`) — Ленинградской областью (`47`), Севастополя (`92`) — Крымом (`91`). Это удобно для одного общего флага, но ломает точные ограничения участия, статистику и городские лидерборды. Хранить исходный код региона, а объединение задавать отдельным `map_group_id`.
2. Нормализовать географию. Поле `city` сейчас является произвольной строкой, координат нет, поэтому любой флаг ставится в геометрический центр региона. Ввести справочники `Region` и `City`, координаты и проверенную точку проекции на SVG-карту.
3. Исправить доступность карты: `role="application"` и `tabindex` не дают клавиатурного выбора регионов. Нужны альтернативный список/поиск событий, навигация стрелками или tab по маркерам, видимый focus, корректные live-объявления и тест с NVDA/клавиатурой.
4. Добавить корректные русские формы числительных (`1 событие`, `2 события`, `5 событий`) и единый формат дат/часовых поясов.
5. Обрабатывать HTTP-ошибки до `response.json()`, показывать состояния загрузки/ошибки отдельно для карты и событий, добавить retry. Сейчас `loadEvents()` не проверяет `response.ok`.
6. Разбить `frontend/src/main.ts` на модули: API, состояние фильтров, карта, маркеры, диалоги, формы, геопроекция. Сейчас вся логика находится в одном файле.
7. Убрать дублирующие/переопределяющие блоки CSS и ввести дизайн-токены компонентов. Это снизит риск случайной поломки адаптивности.
8. Оптимизировать загрузку: production bundle сейчас `556,61 kB` до gzip (`147,42 kB` gzip). Рассмотреть lazy import Three.js-карты, отдельный доступный HTML-список и загрузку WebGL после первого контента.
9. Не загружать шрифты критическим `@import` с внешнего домена: self-host или системный fallback, политика CSP и корректное поведение без сети.
10. Добавить пагинацию/лимиты API и индексы по `published`, `starts_at`, `ends_at`, source ID. Сейчас API безусловно сериализует все будущие события.
11. Вынести сериализацию/валидацию API из представлений, добавить единый JSON-формат ошибок и версионировать публичный API (`/api/v1/`).
12. Усилить тесты: скрытые/прошедшие события, фильтрация, невалидные даты и JSON, CSRF, XSS-строки, admin action, алиасы регионов, ошибки внешних источников, мобильный viewport и клавиатурный сценарий.
13. Исправить контракт сборки после разделения каталогов: backend-проверки на чистом checkout не должны выдавать `staticfiles.W004`, frontend должен атомарно собираться в `backend/static/dist/`, а generated bundle не должен попадать в Git.

### P2 — эксплуатация и качество данных

1. Добавить structured logging, error monitoring, health/readiness endpoints и метрики длительности импорта.
2. Ввести резервное копирование БД, проверку восстановления и политику удаления контактных данных из предложений.
3. Добавить `robots.txt`, sitemap, Open Graph и индексируемый HTML-каталог событий; одна WebGL-карта не должна быть единственным способом найти CTF.
4. Добавить веб-аналитику с минимизацией данных и без передачи email/IP сторонним системам.
5. Документировать deployment, переменные окружения, обновление зависимостей и откат миграций.

## 4. Целевая модель данных

Сначала следует стабилизировать модель, иначе фильтры, импорт и статистика будут постоянно переделываться.

### География

- `Region`: официальный код, название, федеральный округ, `map_group_id`.
- `City`: название, регион, широта/долгота, нормализованное имя, `map_x/map_y` для текущего SVG, источник координат и признак ручной проверки.
- `Venue`: город, адрес, координаты, название площадки; опционально.
- Онлайн-событие не обязано иметь город. Очное событие обязано иметь проверенный город; гибридное — город очной части.

Геокодирование выполнять при импорте/редактировании, а не в браузере при каждом открытии. Администратор должен видеть точку на preview-карте и иметь возможность поправить её вручную.

### События

Расширить `Event`:

- `participation_mode`: online / onsite / hybrid;
- `game_format`: jeopardy / attack-defense / hack-quest / mixed / other;
- `stage`: independent / qualifier / semifinal / final;
- `audience`: open / school / student / academic / invited / individual;
- начало и конец регистрации отдельно от начала и конца игры;
- город/площадка, возрастные и командные ограничения;
- `source`, `source_url`, `external_id`, `last_synced_at`, `sync_status`;
- `participants_count`, `participants_updated_at` и явная подпись «по данным источника»;
- статус публикации: draft / needs_review / published / rejected / archived;
- `parent_event` для связи отборочного этапа с финалом.

Не смешивать два смысла слова «формат»: online/очность и Jeopardy/Attack-Defense должны быть разными полями и разными фильтрами.

### Региональные ограничения

- `EligibilityRule`: событие, тип include/exclude, регион, необязательное пояснение.
- Базовая политика события: `all_allowed`, `only_listed` или `all_except_listed`.
- Более общие ограничения (страна, возраст, школьники, студенты, приглашённые) хранить отдельно, а не кодировать в описании.
- В админке показывать итог до публикации: «допущено 12 регионов, исключено 3», проверять противоречия.

На карте при выборе события: город проведения выделять основным акцентом, допущенные регионы — вторичным цветом, исключённые — приглушать/штриховать. Всегда добавлять легенду и текстовый список, чтобы значение не передавалось одним цветом.

### Источники и дедупликация

- `ImportSource`: CTFtime, CTFNews/календарь, ручной ввод.
- `ExternalEvent`: source + external ID, checksum, raw JSON, timestamps, связь с `Event`.
- `ImportRun` и `ImportIssue`: состояние запуска, ошибки, число новых/обновлённых/конфликтных записей.
- Сначала дедупликация по `(source, external_id)`, затем кандидаты по нормализованному названию и пересечению дат; неоднозначные совпадения — только в очередь модерации.
- Внешнее обновление не должно молча затирать ручную правку: хранить provenance по полям или флаг `locked_by_editor`.

## 5. Дорожная карта

### Этап 0. Безопасная база и тестовый контур

1. Выполнить P0-аудит зависимостей и production settings.
2. Поднять PostgreSQL-конфигурацию, миграции и резервное копирование.
3. Настроить Ruff/форматирование Python, ESLint или эквивалент для TypeScript, CI и Playwright smoke-тест.
4. Добавить тестовые фабрики и фикстуры событий/городов.
5. Зафиксировать baseline производительности и доступности.

Результат: проект безопасно разворачивается и изменения можно проверять автоматически.

### Этап 1. Данные, города и дружелюбная админка

1. Создать справочники регионов/городов и миграцию без потери существующих событий.
2. Отделить визуальные composite groups от кодов субъектов.
3. Расширить модель события полями из раздела 4.
4. Перестроить admin в короткий мастер из секций: «Что», «Когда», «Где», «Кому можно», «Ссылки», «Публикация».
5. Добавить autocomplete, календарные виджеты, preview маркера, итог ограничений, предупреждения о дублях и обязательность города для onsite.
6. Создать отдельный inbox импортов/предложений с действиями «сравнить», «объединить», «опубликовать», «отклонить»; bulk-action должен быть транзакционным и выдавать отчёт об ошибках.
7. Добавить роли: редактор, модератор, администратор; использовать least privilege.

Результат: редактор без знания внутренней модели может корректно добавить CTF за несколько минут.

### Этап 2. Карточка события, умный фильтр и доступный каталог

1. Сделать полноценную карточку/URL события: описание, этап, два вида формата, даты игры и регистрации, место, ограничения, организатор, первоисточник, число команд и кнопки регистрации/поделиться.
2. Добавить фильтры по диапазону дат, `participation_mode`, `game_format`, этапу, аудитории, региону допуска и состоянию регистрации.
3. Синхронизировать фильтры с URL, сохранять их при открытии карточки, добавить быстрые пресеты «в эти выходные», «онлайн», «для школьников», «рядом».
4. Ранжировать результаты объяснимо: регистрация открыта → подходит пользователю → скоро начнётся → ближе географически. Показывать причину рекомендации, а не скрытый «умный» скоринг.
5. Сделать HTML-список равноправным карте: поиск и карточки должны работать без WebGL и с клавиатуры.
6. Покрыть фильтры unit-тестами и e2e-сценариями, включая пустые состояния.

Результат: пользователь за 2–3 действия находит CTF, в котором действительно может участвовать.

### Этап 3. Городские маркеры и огни флагов

1. Если указан проверенный город, ставить флаг в его точке; иначе явно использовать центр региона с подписью «точное место не указано».
2. При близком масштабе раскрывать события по городам, затем отдельные флаги; избегать наложений через cluster/spiderfy.
3. Ввести состояния огней: спокойный, скоро, регистрация закрывается, идёт сейчас. Интенсивность зависит от времени, а детализация — от масштаба карты.
4. Анимировать только `transform`/`opacity`, ограничивать частоту кадров, отключать свечение/волну при `prefers-reduced-motion` и на слабых устройствах.
5. Сделать статус различимым не только цветом: форма/иконка, подпись и легенда.
6. Провести тесты на реальных мобильных устройствах и проверить INP < 200 ms, отсутствие заметного CLS и приемлемый расход батареи.

Результат: приближение карты раскрывает реальные города, а огни помогают понять срочность, не превращаясь в визуальный шум.

### Этап 4. Агрегатор CTFtime и CTFNews

1. Начать с официального CTFtime API: импорт событий по временному окну, карточка по ID, организаторы, формат, onsite/location, restrictions, URL и `participants`.
2. Синхронизировать по расписанию с backoff, timeout, User-Agent, кешем и ограничением частоты; импортировать в draft/needs_review, а не сразу публиковать.
3. Соблюсти ограничение CTFtime: API предназначен для анализа и мобильных приложений и не разрешает делать клон сервиса. Использовать данные как один из источников карты, сохранять атрибуцию и ссылку на оригинал; при сомнении согласовать объём с CTFtime.
4. Число зарегистрированных команд показывать только если поле `participants` присутствует, вместе с временем обновления. Это snapshot источника, не гарантия фактического приезда.
5. Для CTFNews сначала согласовать допустимый канал. На сайте обнаружен публичный календарь, встроенный из Google Calendar, но отдельный документированный API не найден. Предпочесть официальный календарный feed/сотрудничество; HTML scraping включать только после проверки robots/условий и с низкой частотой.
6. Добавить ручную модерацию, предпросмотр diff, дедупликацию и алерты при систематических ошибках источника.
7. Не импортировать в карту новости, которые не являются соревнованиями; связь `NewsItem → Event` можно добавить отдельно.

Справочные точки на момент аудита:

- CTFtime API: <https://ctftime.org/api/>
- пример JSON события с `participants`: <https://ctftime.org/api/v1/events/165/>
- календарь CTFNews: <https://ctfnews.ru/calendar/>

Результат: новые соревнования регулярно попадают в очередь модерации, а не зависят только от ручного ввода.

### Этап 5. Поездка на очный CTF

1. Для onsite/hybrid-события с проверенным городом показать блок «Спланировать поездку».
2. Сформировать внешние ссылки: гостиницы на даты соревнования, маршрут до площадки, поиск еды/транспорта рядом. Начать с обычных URL/deep links Яндекс Карт/Путешествий, без сбора и перепродажи данных пользователя.
3. Не показывать блок для online-событий и событий без точного города.
4. Явно предупредить, что цены и наличие определяет внешний сервис; открывать ссылку только по действию пользователя.
5. Любую партнёрскую монетизацию помечать и проверять юридически отдельно.

Результат: карточка очного CTF помогает перейти от интереса к реальной поездке без разработки собственного сервиса бронирования.

### Этап 6. Команды, результаты и городские лидерборды

1. Создать `Team`, `TeamAlias`, `TeamHomeCity`, `EventResult`; внешние ID и источник обязательны для импортированных результатов.
2. Не считать одинаковые названия одной командой автоматически. Слияние алиасов — через уверенное правило или модератора, с журналом изменений.
3. Импортировать публичные результаты CTFtime после окончания событий; хранить место, баллы, дату snapshot и ссылку на источник.
4. Определить лидерборд города заранее: по домашнему городу команды, с отдельными срезами за год/всё время и прозрачной формулой. Место проведения CTF — фильтр статистики, а не автоматически «город команды».
5. Сделать две страницы: «команды из города» и «результаты соревнований, прошедших в городе», чтобы не смешивать разные смыслы.
6. Добавить механизм исправления принадлежности команды к городу и отметку неполных данных.

Результат: лидерборд можно объяснить и воспроизвести из источников, а команды малых городов получают видимость.

### Этап 7. Маршруты команд на карте

1. Строить маршрут только по публичным результатам очных событий с известным городом и хронологией.
2. Называть визуализацию «предполагаемый маршрут участия», потому что результат не доказывает физический перелёт или способ передвижения.
3. Онлайн-события исключать из географического маршрута или показывать отдельно без линии поездки.
4. Агрегировать на уровне команды; не отслеживать отдельных участников и не показывать текущую геопозицию.
5. По клику на сегмент показывать два события, даты и источники. Ограничить период/число линий, добавить фильтр команды и режим reduced motion.
6. Сначала выпустить статичную хронологию, затем анимированные дуги после проверки производительности и понятности.

Результат: карта показывает историю CTF-сообщества без ложного заявления о реальном трекинге людей.

## 6. Текущая и целевая архитектура

- Физическое разделение завершено: Django находится в `backend/`, Three.js/Vite — в `frontend/`; не возвращать исходники в корень.
- Django остаётся основным приложением и админкой.
- PostgreSQL хранит события, географию, источники, команды и результаты.
- Периодические импорты сначала можно запускать management commands по cron/systemd timer. Очередь Celery/RQ добавлять только когда появятся долгие параллельные задачи и подтверждённая нагрузка.
- Внешние клиенты изолировать в `events/integrations/` с typed DTO, timeout, retry, rate limit и fixture-тестами.
- Публичное API версионировать и отдавать только необходимые поля; агрегаты/фильтры считать на сервере при росте данных.
- Frontend разделить на API client, store/state, map renderer, projection, markers, filters, dialogs и accessible list.
- Сырые ответы внешних сервисов не отдавать клиенту; хранить ограниченно для диагностики и дедупликации.

## 7. Порядок релизов

1. Release A: P0 + CI + production settings.
2. Release B: нормализованные регионы/города + новая админка.
3. Release C: карточки, фильтры и доступный каталог.
4. Release D: городские флаги, огни и региональные ограничения.
5. Release E: CTFtime importer + число участников; CTFNews после согласования канала.
6. Release F: travel links.
7. Release G: команды, результаты и городские лидерборды.
8. Release H: предполагаемые маршруты участия команд.

Каждый release должен иметь миграцию, тесты, rollback/backup plan, обновление README и проверку на desktop/mobile/keyboard/reduced motion.

## 8. Проверки, выполненные во время аудита

- `npm run build` — успешно; есть предупреждение о chunk > 500 kB.
- `npm audit` — 0 известных уязвимостей в npm-зависимостях.
- `(cd backend && python manage.py check)` — успешно после frontend build; на чистом checkout до build обнаружено `staticfiles.W004`.
- `(cd backend && python manage.py check --deploy)` — по исходному аудиту 6 предупреждений безопасности; реструктуризация production settings не исправила.
- `(cd backend && python manage.py makemigrations --check --dry-run)` — изменений нет.
- `(cd backend && python manage.py test -v 2)` — 3/3 теста успешно после pull.
- `pip-audit -r backend/requirements.txt` — по исходному аудиту 24 записи для Django 5.1.7; версия зависимости после pull не изменилась.
- `(cd frontend && npm ci && npm run build)` — успешно после pull; bundle остался `556,61 kB` до gzip (`147,42 kB` gzip), предупреждение о chunk > 500 kB сохраняется.
- `(cd frontend && npm audit)` — 0 известных уязвимостей после pull.
- Chromium smoke-test после миграций — HTTP 200, canvas загружен, 88 вариантов региона, диалог предложения открывается, ошибок страницы/HTTP нет.

## 9. Definition of Done для новой функции

- есть критерии приёмки и понятное состояние ошибки/пустого результата;
- модель данных не кодирует разные понятия в одном поле;
- добавлены backend/unit/e2e-тесты по риску изменения;
- нет секретов и персональных данных в логах;
- клавиатура, screen reader и `prefers-reduced-motion` не являются вторичными сценариями;
- интерактивные цели не меньше 44×44 px, статус не передаётся только цветом;
- внешние данные имеют источник, время обновления и стратегию деградации;
- миграция обратима либо имеет документированный backup/restore plan;
- frontend `npm run build`, затем backend `manage.py check`, проверка миграций и тесты проходят из своих рабочих каталогов;
- README и `AGENTS.md` обновлены, если изменились команды или архитектурные правила.
